<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/app_background"
    android:paddingTop="@dimen/dimen_padding_top">

    <!-- Top Bar với Back Button và Dropdown Menu -->
    <ImageButton
        android:id="@+id/button_back"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="16dp"
        android:background="@drawable/ripple_image_button"
        android:scaleType="fitXY"
        android:src="@drawable/icon_history"
        android:tint="@color/gray_big_icon"
        app:layout_constraintBottom_toBottomOf="@id/dropdown_menu_report"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/dropdown_menu_report"
        style="@style/Widget.App.ExposedDropdownMenu"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:layout_marginTop="16dp"
        android:padding="0dp"
        app:layout_constraintEnd_toStartOf="@+id/button_share"
        app:layout_constraintStart_toEndOf="@id/button_back"
        app:layout_constraintTop_toTopOf="parent">

        <AutoCompleteTextView
            android:id="@+id/auto_complete_report_type"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/blue_outline_rounded_background"
            android:gravity="center"
            android:inputType="none"
            android:paddingVertical="0dp"
            android:textAlignment="center"
            android:paddingStart="4dp"
            android:text="Financial statement"
            android:textColor="@color/blue_bg"
            android:textStyle="bold" />
    </com.google.android.material.textfield.TextInputLayout>

    <ImageButton
        android:id="@+id/button_share"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="16dp"
        android:background="@drawable/ripple_image_button"
        android:scaleType="fitXY"
        android:src="@drawable/ic_share_code"
        android:tint="@color/gray_big_icon"
        app:layout_constraintBottom_toBottomOf="@id/dropdown_menu_report"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Container cho Fragment con -->
    <FrameLayout
        android:id="@+id/fragment_container"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/dropdown_menu_report" />

</androidx.constraintlayout.widget.ConstraintLayout>

